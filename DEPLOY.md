# 🚀 Деплой приложения ТИТАН

## Вариант 1: Render.com (Бесплатно, рекомендуется)

### Шаг 1: Подготовка проекта

1. Создайте аккаунт на https://render.com
2. Установите Git (если ещё не установлен)
3. Инициализируйте Git репозиторий:

```bash
git init
git add .
git commit -m "Initial commit"
```

4. Загрузите на GitHub:
   - Создайте репозиторий на https://github.com
   - Выполните команды:
   ```bash
   git remote add origin https://github.com/ваш-username/gym-app.git
   git branch -M main
   git push -u origin main
   ```

### Шаг 2: Создайте файл для продакшена

Создайте файл `render.yaml` (уже создан ниже)

### Шаг 3: Деплой на Render

1. Войдите на https://render.com
2. Нажмите "New +" → "Web Service"
3. Подключите ваш GitHub репозиторий
4. Render автоматически определит настройки из `render.yaml`
5. Нажмите "Create Web Service"

### Шаг 4: Настройте переменные окружения

В настройках сервиса на Render добавьте:
- `JWT_SECRET`: ваш_секретный_ключ_для_продакшена
- `NODE_ENV`: production

### Готово! 🎉

Ваше приложение будет доступно по адресу:
`https://ваше-приложение.onrender.com`

---

## Вариант 2: Railway.app (Бесплатно с лимитами)

### Шаг 1: Подготовка

1. Создайте аккаунт на https://railway.app
2. Загрузите проект на GitHub (см. Вариант 1)

### Шаг 2: Деплой

1. Войдите на Railway
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Railway автоматически определит Node.js приложение

### Шаг 3: Настройки

1. Добавьте переменные окружения:
   - `JWT_SECRET`: ваш_секретный_ключ
   - `NODE_ENV`: production
   - `PORT`: 3001

2. В настройках добавьте команду запуска:
   ```
   npm run build && npm run server
   ```

### Готово!

Приложение будет доступно по адресу Railway.

---

## Вариант 3: Vercel (для фронтенда) + Render (для бэкенда)

### Фронтенд на Vercel:

1. Создайте аккаунт на https://vercel.com
2. Подключите GitHub репозиторий
3. Настройте:
   - Build Command: `npm run build`
   - Output Directory: `dist`
4. Добавьте переменную окружения:
   - `VITE_API_URL`: https://ваш-backend.onrender.com

### Бэкенд на Render:

1. Создайте Web Service на Render
2. Укажите команду запуска: `npm run server`
3. Добавьте переменные окружения

---

## Вариант 4: VPS (DigitalOcean, Hetzner, etc.)

Для полного контроля и больших нагрузок.

### Шаг 1: Арендуйте VPS

- DigitalOcean: от $4/месяц
- Hetzner: от €4/месяц
- Timeweb (RU): от 200₽/месяц

### Шаг 2: Настройте сервер

```bash
# Подключитесь к серверу
ssh root@ваш-ip

# Установите Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# Установите PM2
npm install -g pm2

# Клонируйте проект
git clone https://github.com/ваш-username/gym-app.git
cd gym-app

# Установите зависимости
npm install

# Соберите фронтенд
npm run build

# Инициализируйте БД
npm run init-db

# Запустите с PM2
pm2 start server/index.js --name gym-backend
pm2 startup
pm2 save
```

### Шаг 3: Настройте Nginx

```bash
sudo apt install nginx

# Создайте конфигурацию
sudo nano /etc/nginx/sites-available/gym
```

Добавьте:
```nginx
server {
    listen 80;
    server_name ваш-домен.com;

    # Фронтенд
    location / {
        root /root/gym-app/dist;
        try_files $uri $uri/ /index.html;
    }

    # API
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

```bash
# Активируйте конфигурацию
sudo ln -s /etc/nginx/sites-available/gym /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### Шаг 4: Настройте SSL (HTTPS)

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d ваш-домен.com
```

---

## 🔧 Важные изменения для продакшена

### 1. Обновите vite.config.js для продакшена

Уже настроено в проекте.

### 2. Измените JWT_SECRET

В `.env` на сервере используйте сложный секретный ключ:
```
JWT_SECRET=очень_длинный_случайный_ключ_минимум_32_символа
```

### 3. Настройте CORS

В `server/index.js` укажите ваш домен:
```javascript
app.use(cors({
  origin: 'https://ваш-домен.com'
}));
```

---

## 📱 Использование после деплоя

### Доступ к приложению:

- **Админ-панель**: https://ваш-домен.com
- **Сканер QR**: https://ваш-домен.com/scanner

### Для сканирования на телефоне:

1. Откройте `https://ваш-домен.com/scanner` на телефоне
2. Разрешите доступ к камере
3. Сканируйте QR-коды клиентов

### Для работы на планшете у входа:

1. Откройте сканер в полноэкранном режиме
2. Поставьте планшет на стойку
3. Клиенты показывают QR-код камере

---

## 💰 Стоимость

### Бесплатные варианты:
- **Render.com**: Бесплатно (с ограничениями)
- **Railway.app**: $5 бесплатно в месяц
- **Vercel**: Бесплатно для фронтенда

### Платные варианты:
- **VPS**: от $4-5/месяц (полный контроль)
- **Render Pro**: от $7/месяц (без ограничений)

---

## 🔒 Безопасность для продакшена

1. **Измените пароль администратора** после первого входа
2. **Используйте HTTPS** (SSL сертификат)
3. **Смените JWT_SECRET** на сложный ключ
4. **Настройте CORS** только для вашего домена
5. **Регулярно делайте бэкапы** базы данных

---

## 📊 Мониторинг

### На Render/Railway:
- Встроенные логи и метрики
- Автоматический перезапуск при падении

### На VPS с PM2:
```bash
# Просмотр логов
pm2 logs gym-backend

# Мониторинг
pm2 monit

# Статус
pm2 status
```

---

## 🔄 Обновление приложения

### На Render/Railway:
1. Сделайте изменения в коде
2. Закоммитьте в Git
3. Запушьте на GitHub
4. Автоматический деплой!

### На VPS:
```bash
cd gym-app
git pull
npm install
npm run build
pm2 restart gym-backend
```

---

## ❓ Рекомендация

Для начала используйте **Render.com**:
- ✅ Бесплатно
- ✅ Простой деплой
- ✅ Автоматические обновления
- ✅ SSL из коробки
- ✅ Не нужно настраивать сервер

Когда вырастете - переходите на VPS для большей производительности.
