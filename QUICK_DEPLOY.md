# 🚀 Быстрый деплой ТИТАН

## Самый простой способ (5 минут)

### 1. Загрузите на GitHub

```bash
# Инициализируйте Git
git init
git add .
git commit -m "ТИТАН - готов к деплою"

# Создайте репозиторий на https://github.com
# Затем выполните:
git remote add origin https://github.com/ваш-username/titan-gym.git
git branch -M main
git push -u origin main
```

### 2. Деплой на Render.com

1. Откройте https://render.com
2. Зарегистрируйтесь (можно через GitHub)
3. Нажмите **"New +"** → **"Web Service"**
4. Подключите ваш GitHub репозиторий
5. Настройки:
   - **Name**: titan-gym
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm run server`
6. Добавьте переменные окружения:
   - `NODE_ENV` = `production`
   - `JWT_SECRET` = `ваш_секретный_ключ_минимум_32_символа`
7. Нажмите **"Create Web Service"**

### 3. Готово! 🎉

Через 2-3 минуты ваше приложение будет доступно по адресу:
```
https://titan-gym.onrender.com
```

---

## 📱 Использование

### На компьютере:
- Админ-панель: `https://ваш-адрес.onrender.com`
- Логин: `admin`
- Пароль: `admin123`

### На телефоне/планшете:
- Сканер: `https://ваш-адрес.onrender.com/scanner`
- Разрешите доступ к камере
- Сканируйте QR-коды клиентов

---

## ⚠️ Важно!

### После деплоя:

1. **Смените пароль администратора**
   - Войдите в систему
   - Измените пароль в настройках

2. **Сохраните адрес приложения**
   - Добавьте в закладки
   - Поделитесь с сотрудниками

3. **Бэкапы базы данных**
   - Render сохраняет данные
   - Но лучше периодически скачивать файл `gym.db`

---

## 🔄 Обновление приложения

Когда вы внесёте изменения в код:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Render автоматически задеплоит новую версию!

---

## 💰 Бесплатный план Render

- ✅ Бесплатно навсегда
- ⚠️ Засыпает после 15 минут неактивности
- ⚠️ Первый запрос после сна - медленный (30 сек)
- ✅ Для небольшого зала - достаточно!

### Если нужна постоянная работа:

Upgrade до платного плана ($7/месяц):
- Не засыпает
- Быстрая работа 24/7
- Больше ресурсов

---

## 🆘 Проблемы?

### Приложение не открывается:
- Подождите 30 секунд (первый запуск после сна)
- Проверьте логи на Render

### Камера не работает:
- Нужен HTTPS (Render даёт автоматически)
- Разрешите доступ к камере в браузере

### База данных пустая:
- Render создаёт новую БД при деплое
- Добавьте клиентов заново
- Или загрузите старый файл `gym.db`

---

## 📖 Подробная инструкция

Смотрите [DEPLOY.md](DEPLOY.md) для других вариантов деплоя:
- Railway.app
- Vercel + Render
- VPS (свой сервер)
