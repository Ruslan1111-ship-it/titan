# Система учёта посещений тренажёрного зала

Полнофункциональное веб-приложение для управления клиентами, тренерами и отслеживания посещений тренажёрного зала с использованием QR-кодов.

## Возможности

### 🔐 Аутентификация
- Безопасный вход для администраторов
- JWT-токены для защиты API

### 👥 Управление клиентами
- Добавление, редактирование и удаление клиентов
- Хранение: ФИО, телефон, дата регистрации
- Управление статусом абонемента (активен/неактивен, дата окончания)
- Привязка к персональному тренеру
- Генерация уникального QR-кода для каждого клиента

### 💪 Управление тренерами
- Добавление и редактирование тренеров
- ФИО, контакты, специализация
- Статистика по клиентам и посещениям

### 📱 QR-код сканирование
- Генерация уникальных QR-кодов с UUID
- Веб-сканер с доступом к камере
- Проверка статуса абонемента при входе
- Автоматическая регистрация посещения
- Отображение информации о клиенте и тренере

### 📊 Журнал посещений
- История всех посещений с датой и временем
- Фильтрация по датам и клиентам
- Просмотр посещений по каждому клиенту

### 📈 Аналитика и отчёты
- Общая статистика (посещения за день/неделю/месяц)
- Топ самых активных клиентов
- Процент посещаемости по активным абонементам
- Статистика по тренерам (количество клиентов, посещений)
- График посещений по дням
- Анализ пиковых часов

### 🎨 Интерфейс
- Современный и интуитивный дизайн
- Адаптивная вёрстка для всех устройств
- Отдельный экран для сканирования QR-кодов

## Технологии

### Backend
- **Node.js** + **Express** - серверная часть
- **SQLite** (better-sqlite3) - база данных
- **JWT** - аутентификация
- **bcryptjs** - хеширование паролей
- **QRCode** - генерация QR-кодов
- **UUID** - уникальные идентификаторы

### Frontend
- **React 18** - UI библиотека
- **React Router** - маршрутизация
- **TailwindCSS** - стилизация
- **Lucide React** - иконки
- **Recharts** - графики и диаграммы
- **date-fns** - работа с датами
- **jsQR** - сканирование QR-кодов

## Установка и запуск

### Для Windows (рекомендуется)

**Смотрите подробную инструкцию:** [WINDOWS_SETUP.md](WINDOWS_SETUP.md)

Быстрый старт:
```bash
# 1. Установите Node.js с https://nodejs.org/
# 2. Установите зависимости
npm install

# 3. Инициализируйте базу данных
npm run init-db

# 4. Запустите приложение
npm run dev
```

### Для macOS/Linux

**Смотрите:** [SETUP.md](SETUP.md)

```bash
# Для macOS требуется Xcode Command Line Tools
xcode-select --install

npm install
npm run init-db
npm run dev
```

### Доступ к приложению

После запуска откройте браузер:
- **Админ-панель:** http://localhost:3000
- **Сканер QR:** http://localhost:3000/scanner
- **Backend API:** http://localhost:3001/api

**Учётные данные по умолчанию:**
- Логин: `admin`
- Пароль: `admin123`

### Настройка окружения

Файл `.env` создаётся автоматически. При необходимости отредактируйте:
```
PORT=3001
JWT_SECRET=ваш_секретный_ключ_измените_в_продакшене
DATABASE_PATH=./gym.db
NODE_ENV=development
```

## Структура проекта

```
windsurf-project/
├── server/                 # Backend
│   ├── routes/            # API маршруты
│   │   ├── auth.js       # Аутентификация
│   │   ├── clients.js    # Управление клиентами
│   │   ├── trainers.js   # Управление тренерами
│   │   ├── visits.js     # Посещения
│   │   └── analytics.js  # Аналитика
│   ├── middleware/        # Middleware
│   │   └── auth.js       # JWT проверка
│   ├── database.js        # Подключение к БД
│   ├── initDb.js         # Инициализация БД
│   └── index.js          # Главный файл сервера
├── src/                   # Frontend
│   ├── components/        # React компоненты
│   │   └── Layout.jsx    # Основной layout
│   ├── pages/            # Страницы
│   │   ├── Login.jsx     # Вход
│   │   ├── Dashboard.jsx # Главная панель
│   │   ├── Clients.jsx   # Клиенты
│   │   ├── Trainers.jsx  # Тренеры
│   │   ├── Visits.jsx    # Посещения
│   │   ├── Analytics.jsx # Аналитика
│   │   └── Scanner.jsx   # Сканер QR
│   ├── utils/            # Утилиты
│   │   └── api.js        # API клиент
│   ├── App.jsx           # Главный компонент
│   ├── main.jsx          # Точка входа
│   └── index.css         # Стили
├── package.json
├── vite.config.js
├── tailwind.config.js
└── README.md
```

## API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход в систему

### Клиенты
- `GET /api/clients` - Получить всех клиентов
- `GET /api/clients/:id` - Получить клиента по ID
- `GET /api/clients/uuid/:uuid` - Получить клиента по UUID
- `POST /api/clients` - Создать клиента
- `PUT /api/clients/:id` - Обновить клиента
- `DELETE /api/clients/:id` - Удалить клиента
- `GET /api/clients/:id/qrcode` - Получить QR-код клиента

### Тренеры
- `GET /api/trainers` - Получить всех тренеров
- `POST /api/trainers` - Создать тренера
- `PUT /api/trainers/:id` - Обновить тренера
- `DELETE /api/trainers/:id` - Удалить тренера

### Посещения
- `GET /api/visits` - Получить посещения (с фильтрами)
- `POST /api/visits/checkin` - Зарегистрировать посещение (сканирование QR)
- `GET /api/visits/client/:clientId` - История посещений клиента
- `DELETE /api/visits/:id` - Удалить посещение

### Аналитика
- `GET /api/analytics/stats` - Общая статистика
- `GET /api/analytics/top-clients` - Топ активных клиентов
- `GET /api/analytics/trainer-stats` - Статистика тренеров
- `GET /api/analytics/visits-chart` - График посещений
- `GET /api/analytics/peak-hours` - Пиковые часы

## Безопасность

- ✅ QR-коды содержат только UUID, без персональных данных
- ✅ Пароли хешируются с помощью bcrypt
- ✅ JWT токены для защиты API
- ✅ Проверка авторизации на всех защищённых маршрутах
- ✅ Валидация данных на сервере

## Использование

### Добавление клиента
1. Войдите в систему (admin/admin123)
2. Перейдите в раздел "Клиенты"
3. Нажмите "Добавить клиента"
4. Заполните форму и сохраните
5. Нажмите на иконку QR-кода для генерации и скачивания

### Регистрация посещения
1. Откройте сканер QR (отдельная вкладка/устройство)
2. Разрешите доступ к камере
3. Отсканируйте QR-код клиента
4. Система автоматически проверит абонемент и зарегистрирует посещение

### Просмотр аналитики
1. Перейдите в раздел "Аналитика"
2. Выберите период (неделя/месяц)
3. Просматривайте графики и статистику

## Развёртывание

### Локальное развёртывание
Приложение готово к запуску локально. База данных SQLite создаётся автоматически.

### Облачное развёртывание
Для развёртывания на хостинге:

1. **Backend:** Разверните на Node.js хостинге (Heroku, Railway, Render)
2. **Frontend:** Соберите и разверните на статическом хостинге (Netlify, Vercel)
3. **База данных:** Используйте PostgreSQL для продакшена (требуется адаптация кода)

## Лицензия

MIT

## Поддержка

Для вопросов и предложений создавайте issue в репозитории.
