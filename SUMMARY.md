# Система учёта посещений тренажёрного зала - Сводка проекта

## ✅ Что создано

Полнофункциональное веб-приложение для управления тренажёрным залом с QR-кодами.

### 🗄️ База данных (SQLite)

**Таблицы:**
- `admins` - администраторы системы
- `trainers` - тренеры (ФИО, телефон, специализация)
- `clients` - клиенты (ФИО, телефон, UUID для QR, статус абонемента, тренер)
- `visits` - журнал посещений (клиент, дата, время)

**Файлы:**
- `server/database.js` - подключение к БД
- `server/initDb.js` - инициализация и создание таблиц

### 🔧 Backend API (Express.js)

**Маршруты:**

1. **Аутентификация** (`server/routes/auth.js`)
   - POST `/api/auth/login` - вход в систему

2. **Клиенты** (`server/routes/clients.js`)
   - GET `/api/clients` - список всех клиентов
   - GET `/api/clients/:id` - клиент по ID
   - GET `/api/clients/uuid/:uuid` - клиент по UUID (для QR)
   - POST `/api/clients` - создать клиента
   - PUT `/api/clients/:id` - обновить клиента
   - DELETE `/api/clients/:id` - удалить клиента
   - GET `/api/clients/:id/qrcode` - сгенерировать QR-код

3. **Тренеры** (`server/routes/trainers.js`)
   - GET `/api/trainers` - список тренеров
   - POST `/api/trainers` - создать тренера
   - PUT `/api/trainers/:id` - обновить тренера
   - DELETE `/api/trainers/:id` - удалить тренера

4. **Посещения** (`server/routes/visits.js`)
   - GET `/api/visits` - список посещений (с фильтрами)
   - POST `/api/visits/checkin` - регистрация посещения (QR)
   - GET `/api/visits/client/:clientId` - история клиента
   - DELETE `/api/visits/:id` - удалить посещение

5. **Аналитика** (`server/routes/analytics.js`)
   - GET `/api/analytics/stats` - общая статистика
   - GET `/api/analytics/top-clients` - топ клиентов
   - GET `/api/analytics/trainer-stats` - статистика тренеров
   - GET `/api/analytics/visits-chart` - график посещений
   - GET `/api/analytics/peak-hours` - пиковые часы

**Безопасность:**
- JWT токены для аутентификации
- bcrypt для хеширования паролей
- Middleware для проверки авторизации

### 🎨 Frontend (React + TailwindCSS)

**Страницы:**

1. **Login** (`src/pages/Login.jsx`)
   - Форма входа
   - Красивый дизайн с градиентом

2. **Dashboard** (`src/pages/Dashboard.jsx`)
   - Статистика (посещения, клиенты, абонементы)
   - График посещений
   - Быстрые действия

3. **Clients** (`src/pages/Clients.jsx`)
   - Таблица клиентов
   - Добавление/редактирование/удаление
   - Генерация и скачивание QR-кодов
   - Управление абонементами
   - Привязка к тренерам

4. **Trainers** (`src/pages/Trainers.jsx`)
   - Карточки тренеров
   - Добавление/редактирование/удаление
   - Статистика по клиентам

5. **Visits** (`src/pages/Visits.jsx`)
   - Журнал посещений
   - Фильтры по датам
   - Статистика

6. **Analytics** (`src/pages/Analytics.jsx`)
   - Подробная статистика
   - Графики (посещения, пиковые часы)
   - Топ активных клиентов
   - Статистика тренеров

7. **Scanner** (`src/pages/Scanner.jsx`)
   - Сканирование QR через камеру
   - Ручной ввод UUID
   - Проверка абонемента
   - Регистрация посещения
   - Отображение информации о клиенте

**Компоненты:**
- `Layout.jsx` - основной layout с навигацией
- Адаптивный дизайн для всех устройств
- Иконки Lucide React
- Графики Recharts

### 📦 Конфигурация

**Файлы:**
- `package.json` - зависимости и скрипты
- `vite.config.js` - настройки Vite
- `tailwind.config.js` - настройки TailwindCSS
- `.env` - переменные окружения
- `.gitignore` - исключения для Git

### 📚 Документация

- `README.md` - полная документация проекта
- `SETUP.md` - инструкция по установке
- `SUMMARY.md` - этот файл

## 🚀 Как запустить

### Шаг 1: Установить Xcode Command Line Tools

```bash
xcode-select --install
```

### Шаг 2: Установить зависимости

```bash
npm install
```

### Шаг 3: Инициализировать базу данных

```bash
npm run init-db
```

Будет создан администратор:
- Логин: `admin`
- Пароль: `admin123`

### Шаг 4: Запустить приложение

```bash
npm run dev
```

Откроется:
- Frontend: http://localhost:3000
- Backend API: http://localhost:3001/api
- Scanner: http://localhost:3000/scanner

## 📋 Основные функции

### 1. Управление клиентами
- ✅ Добавление клиентов с ФИО, телефоном, датой регистрации
- ✅ Управление статусом абонемента (активен/неактивен)
- ✅ Установка даты окончания абонемента
- ✅ Привязка к персональному тренеру
- ✅ Генерация уникального QR-кода (UUID)
- ✅ Скачивание QR-кода в PNG

### 2. Управление тренерами
- ✅ Добавление тренеров с ФИО, телефоном, специализацией
- ✅ Просмотр количества прикреплённых клиентов
- ✅ Редактирование и удаление

### 3. QR-код система
- ✅ Генерация уникального UUID для каждого клиента
- ✅ QR-код содержит только UUID (без персональных данных)
- ✅ Веб-сканер с доступом к камере
- ✅ Ручной ввод UUID
- ✅ Автоматическая проверка абонемента
- ✅ Регистрация посещения с датой и временем
- ✅ Отображение информации о клиенте и тренере

### 4. Журнал посещений
- ✅ Полная история всех посещений
- ✅ Фильтрация по датам
- ✅ Просмотр по клиентам
- ✅ Информация о тренере
- ✅ Удаление записей

### 5. Аналитика
- ✅ Общая статистика (день/неделя/месяц)
- ✅ Количество посещений
- ✅ Уникальные посетители
- ✅ Активные абонементы
- ✅ Процент посещаемости
- ✅ Топ активных клиентов
- ✅ Статистика по тренерам
- ✅ График посещений по дням
- ✅ Анализ пиковых часов

### 6. Безопасность
- ✅ JWT аутентификация
- ✅ Хеширование паролей (bcrypt)
- ✅ Защита API маршрутов
- ✅ QR-коды без персональных данных
- ✅ Валидация данных

## 🛠️ Технологический стек

### Backend
- Node.js 18+
- Express.js 4.18
- SQLite (better-sqlite3)
- JWT для токенов
- bcryptjs для паролей
- QRCode для генерации
- UUID для идентификаторов

### Frontend
- React 18
- React Router 6
- TailwindCSS 3
- Vite 5
- Lucide React (иконки)
- Recharts (графики)
- date-fns (даты)
- jsQR (сканирование)

## 📁 Структура файлов

```
windsurf-project/
├── server/
│   ├── routes/
│   │   ├── auth.js          # Аутентификация
│   │   ├── clients.js       # API клиентов
│   │   ├── trainers.js      # API тренеров
│   │   ├── visits.js        # API посещений
│   │   └── analytics.js     # API аналитики
│   ├── middleware/
│   │   └── auth.js          # JWT middleware
│   ├── database.js          # Подключение к БД
│   ├── initDb.js           # Инициализация БД
│   └── index.js            # Главный файл сервера
├── src/
│   ├── components/
│   │   └── Layout.jsx       # Layout с навигацией
│   ├── pages/
│   │   ├── Login.jsx        # Страница входа
│   │   ├── Dashboard.jsx    # Главная панель
│   │   ├── Clients.jsx      # Управление клиентами
│   │   ├── Trainers.jsx     # Управление тренерами
│   │   ├── Visits.jsx       # Журнал посещений
│   │   ├── Analytics.jsx    # Аналитика
│   │   └── Scanner.jsx      # Сканер QR
│   ├── utils/
│   │   └── api.js          # API клиент
│   ├── App.jsx             # Главный компонент
│   ├── main.jsx            # Точка входа
│   └── index.css           # Стили
├── package.json
├── vite.config.js
├── tailwind.config.js
├── .env                    # Конфигурация
├── .gitignore
├── README.md               # Документация
├── SETUP.md                # Инструкция установки
└── SUMMARY.md              # Эта сводка
```

## 🎯 Следующие шаги

1. **Установите Xcode Command Line Tools:**
   ```bash
   xcode-select --install
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Инициализируйте базу данных:**
   ```bash
   npm run init-db
   ```

4. **Запустите приложение:**
   ```bash
   npm run dev
   ```

5. **Откройте браузер:**
   - http://localhost:3000 - админ-панель
   - http://localhost:3000/scanner - сканер QR

6. **Войдите в систему:**
   - Логин: `admin`
   - Пароль: `admin123`

## 💡 Рекомендации

1. **Измените пароль администратора** после первого входа
2. **Обновите JWT_SECRET** в `.env` для продакшена
3. **Создайте резервные копии** базы данных (`gym.db`)
4. **Используйте HTTPS** в продакшене
5. **Настройте CORS** для продакшена

## 🐛 Решение проблем

### Ошибка компиляции better-sqlite3
```bash
xcode-select --install
```

### Порты заняты
Измените порты в `.env` и `vite.config.js`

### Камера не работает
- Разрешите доступ к камере в браузере
- Используйте HTTPS или localhost
- Попробуйте ручной ввод UUID

## 📞 Поддержка

Все файлы созданы и готовы к использованию. Следуйте инструкциям в SETUP.md для запуска приложения.

---

**Проект полностью готов к использованию!** 🎉
