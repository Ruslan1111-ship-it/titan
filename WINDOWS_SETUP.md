# 🪟 Установка на Windows

## Быстрый старт для Windows

### Шаг 1: Установите Node.js

1. Скачайте Node.js с официального сайта: https://nodejs.org/
2. Выберите **LTS версию** (рекомендуется)
3. Запустите установщик и следуйте инструкциям
4. Перезагрузите компьютер после установки

Проверьте установку в командной строке (cmd или PowerShell):
```bash
node --version
npm --version
```

### Шаг 2: Установите зависимости

Откройте командную строку (cmd) или PowerShell в папке проекта и выполните:

```bash
npm install
```

**На Windows** пакет `better-sqlite3` устанавливается автоматически с предсобранными бинарными файлами, **без необходимости компиляции**!

### Шаг 3: Инициализируйте базу данных

```bash
npm run init-db
```

Будет создан администратор:
- **Логин:** admin
- **Пароль:** admin123

### Шаг 4: Запустите приложение

```bash
npm run dev
```

Откройте браузер:
- **Админ-панель:** http://localhost:3000
- **Сканер QR:** http://localhost:3000/scanner

---

## 🎯 Что вы получите

### Веб-приложение работает полностью в браузере!

- ✅ **Админ-панель** - управление клиентами и тренерами
- ✅ **Сканер QR** - работает через веб-камеру ноутбука
- ✅ **Аналитика** - графики и отчёты
- ✅ **База данных** - SQLite (локальный файл)

### Как использовать на Windows:

#### Вариант 1: Один ноутбук (рекомендуется для начала)

```
┌─────────────────────────────────┐
│   Windows Ноутбук               │
│                                 │
│  ┌─────────────────────────┐   │
│  │  Браузер (Chrome)       │   │
│  │                         │   │
│  │  Вкладка 1: Админка     │   │
│  │  Вкладка 2: Сканер QR   │   │
│  └─────────────────────────┘   │
│                                 │
│  Веб-камера для сканирования   │
└─────────────────────────────────┘
```

**Использование:**
1. Запустите `npm run dev`
2. Откройте http://localhost:3000 - работайте в админке
3. Откройте http://localhost:3000/scanner в новой вкладке
4. Поставьте ноутбук на входе в зал
5. Клиенты показывают QR-код веб-камере

#### Вариант 2: Несколько устройств в локальной сети

```
┌─────────────────────┐         ┌─────────────────────┐
│  Windows Ноутбук    │         │  Любое устройство   │
│  (Сервер)           │◄────────┤  (Планшет/Телефон)  │
│                     │  Wi-Fi  │                     │
│  npm run dev        │         │  Браузер: Сканер    │
│  192.168.1.100:3000 │         │  192.168.1.100:3000 │
└─────────────────────┘         └─────────────────────┘
```

**Настройка:**

1. Узнайте IP-адрес вашего ноутбука:
   ```bash
   ipconfig
   ```
   Найдите IPv4-адрес (например, 192.168.1.100)

2. Измените `vite.config.js`:
   ```javascript
   export default defineConfig({
     plugins: [react()],
     server: {
       host: '0.0.0.0',  // Добавьте эту строку
       port: 3000,
       proxy: {
         '/api': {
           target: 'http://localhost:3001',
           changeOrigin: true,
         },
       },
     },
   });
   ```

3. Запустите приложение:
   ```bash
   npm run dev
   ```

4. На других устройствах откройте:
   - Админка: `http://192.168.1.100:3000`
   - Сканер: `http://192.168.1.100:3000/scanner`

---

## 🔧 Команды для Windows

### Установка и запуск

```bash
# Установить зависимости
npm install

# Инициализировать базу данных
npm run init-db

# Запустить приложение (frontend + backend)
npm run dev

# Запустить только backend
npm run server

# Запустить только frontend
npm run client

# Собрать для продакшена
npm run build
```

### Остановка приложения

Нажмите `Ctrl + C` в командной строке

---

## 📱 Использование веб-камеры для сканирования

### На Windows ноутбуке:

1. Откройте http://localhost:3000/scanner
2. Браузер запросит доступ к камере - **разрешите**
3. Нажмите "Начать сканирование"
4. Поднесите QR-код клиента к камере
5. Система автоматически распознает и зарегистрирует посещение

### Рекомендации:

- **Браузер:** Google Chrome (лучшая поддержка камеры)
- **Освещение:** Хорошее освещение для чёткого сканирования
- **Расстояние:** 20-30 см от камеры до QR-кода
- **Альтернатива:** Ручной ввод UUID, если камера не работает

---

## 🐛 Решение проблем на Windows

### Ошибка при установке зависимостей

**Проблема:** `npm install` завершается с ошибкой

**Решение:**
```bash
# Очистите кэш npm
npm cache clean --force

# Удалите node_modules
rmdir /s /q node_modules

# Установите заново
npm install
```

### Порты заняты

**Проблема:** Порт 3000 или 3001 уже используется

**Решение:** Измените порты в `.env`:
```
PORT=3002
```

И в `vite.config.js`:
```javascript
server: {
  port: 3001,
}
```

### Камера не работает

**Проблема:** Сканер не видит камеру

**Решение:**
1. Используйте **Chrome** или **Edge** (лучшая поддержка)
2. Разрешите доступ к камере в браузере
3. Проверьте, что камера не используется другим приложением
4. Используйте **ручной ввод UUID** как альтернативу

### База данных не создаётся

**Проблема:** Ошибка при `npm run init-db`

**Решение:**
```bash
# Убедитесь, что файл gym.db не открыт в другой программе
# Удалите старую базу
del gym.db

# Создайте заново
npm run init-db
```

### Приложение не открывается в браузере

**Проблема:** После `npm run dev` браузер не открывается

**Решение:**
Откройте браузер вручную и перейдите на:
- http://localhost:3000

---

## 🚀 Развёртывание на Windows Server

### Для постоянной работы:

1. **Установите приложение как службу Windows** (опционально)
2. **Используйте PM2** для автозапуска:

```bash
# Установите PM2 глобально
npm install -g pm2

# Запустите backend
pm2 start server/index.js --name gym-backend

# Соберите frontend
npm run build

# Используйте nginx или serve для раздачи статики
```

---

## 📋 Чек-лист для Windows

- [ ] Установлен Node.js (версия 16+)
- [ ] Выполнено `npm install` (без ошибок)
- [ ] Выполнено `npm run init-db`
- [ ] Запущено `npm run dev`
- [ ] Открыт браузер http://localhost:3000
- [ ] Выполнен вход (admin/admin123)
- [ ] Протестирован сканер QR с веб-камерой
- [ ] Добавлены тестовые клиенты
- [ ] Сгенерированы QR-коды

---

## 💡 Рекомендации для работы

### Оптимальная настройка:

1. **Ноутбук на входе в зал:**
   - Откройте сканер в полноэкранном режиме (F11)
   - Поставьте ноутбук так, чтобы клиенты могли показать QR-код камере
   - Включите автояркость экрана

2. **Админ-панель:**
   - Работайте с другого компьютера через локальную сеть
   - Или используйте второй монитор

3. **Печать QR-кодов:**
   - Скачайте QR-коды клиентов
   - Распечатайте на принтере
   - Раздайте клиентам или сделайте пластиковые карты

---

## ✅ Готово!

Теперь вы можете запустить приложение на Windows без каких-либо проблем!

```bash
npm install
npm run init-db
npm run dev
```

**Приложение работает полностью в браузере, никаких мобильных приложений не требуется!** 🎉
